/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { bankAccountsDeleteV1BankAccountsId } from "../../funcs/bankAccountsDeleteV1BankAccountsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteV1BankAccountsIdRequest$inboundSchema,
};

export const tool$bankAccountsDeleteV1BankAccountsId: ToolDefinition<
  typeof args
> = {
  name: "bank-accounts-delete-v1-bank-accounts-id",
  description: `Delete a bank account

Delete a bank account by ID for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await bankAccountsDeleteV1BankAccountsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
