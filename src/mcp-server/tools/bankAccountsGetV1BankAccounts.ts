/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { bankAccountsGetV1BankAccounts } from "../../funcs/bankAccountsGetV1BankAccounts.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1BankAccountsRequest$inboundSchema,
};

export const tool$bankAccountsGetV1BankAccounts: ToolDefinition<typeof args> = {
  name: "bank-accounts-get-v1-bank-accounts",
  description: `List all bank accounts

Retrieve a list of bank accounts for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await bankAccountsGetV1BankAccounts(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
