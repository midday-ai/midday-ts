/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { bankAccountsGetV1BankAccountsId } from "../../funcs/bankAccountsGetV1BankAccountsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1BankAccountsIdRequest$inboundSchema,
};

export const tool$bankAccountsGetV1BankAccountsId: ToolDefinition<typeof args> =
  {
    name: "bank-accounts-get-v1-bank-accounts-id",
    description: `Retrieve a bank account

Retrieve a bank account by ID for the authenticated team.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await bankAccountsGetV1BankAccountsId(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
