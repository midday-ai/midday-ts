/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { bankAccountsPatchV1BankAccountsId } from "../../funcs/bankAccountsPatchV1BankAccountsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchV1BankAccountsIdRequest$inboundSchema,
};

export const tool$bankAccountsPatchV1BankAccountsId: ToolDefinition<
  typeof args
> = {
  name: "bank-accounts-patch-v1-bank-accounts-id",
  description: `Update a bank account

Update a bank account by ID for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await bankAccountsPatchV1BankAccountsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
