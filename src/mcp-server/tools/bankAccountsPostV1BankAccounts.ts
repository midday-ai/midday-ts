/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { bankAccountsPostV1BankAccounts } from "../../funcs/bankAccountsPostV1BankAccounts.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostV1BankAccountsRequest$inboundSchema.optional(),
};

export const tool$bankAccountsPostV1BankAccounts: ToolDefinition<typeof args> =
  {
    name: "bank-accounts-post-v1-bank-accounts",
    description: `Create a bank account

Create a new bank account for the authenticated team.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await bankAccountsPostV1BankAccounts(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
