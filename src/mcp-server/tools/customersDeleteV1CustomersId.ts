/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customersDeleteV1CustomersId } from "../../funcs/customersDeleteV1CustomersId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteV1CustomersIdRequest$inboundSchema,
};

export const tool$customersDeleteV1CustomersId: ToolDefinition<typeof args> = {
  name: "customers-delete-v1-customers-id",
  description: `Delete a customer

Delete a customer by ID for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customersDeleteV1CustomersId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
