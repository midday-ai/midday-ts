/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customersGetV1Customers } from "../../funcs/customersGetV1Customers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1CustomersRequest$inboundSchema,
};

export const tool$customersGetV1Customers: ToolDefinition<typeof args> = {
  name: "customers-get-v1-customers",
  description: `List all customers

Retrieve a list of customers for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customersGetV1Customers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
