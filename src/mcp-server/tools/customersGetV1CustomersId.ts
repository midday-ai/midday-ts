/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customersGetV1CustomersId } from "../../funcs/customersGetV1CustomersId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1CustomersIdRequest$inboundSchema,
};

export const tool$customersGetV1CustomersId: ToolDefinition<typeof args> = {
  name: "customers-get-v1-customers-id",
  description: `Retrieve a customer

Retrieve a customer by ID for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customersGetV1CustomersId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
