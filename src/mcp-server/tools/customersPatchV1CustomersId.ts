/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customersPatchV1CustomersId } from "../../funcs/customersPatchV1CustomersId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchV1CustomersIdRequest$inboundSchema,
};

export const tool$customersPatchV1CustomersId: ToolDefinition<typeof args> = {
  name: "customers-patch-v1-customers-id",
  description: `Update a customer

Update a customer by ID for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customersPatchV1CustomersId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
