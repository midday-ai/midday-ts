/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { desktopDownloadUpdate } from "../../funcs/desktopDownloadUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DownloadDesktopUpdateRequest$inboundSchema,
};

export const tool$desktopDownloadUpdate: ToolDefinition<typeof args> = {
  name: "desktop-download-update",
  description: `Download desktop app update artifact

Proxies the download of a desktop app update artifact from GitHub releases. Only URLs pointing to the midday-ai/midday repository are accepted.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await desktopDownloadUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
