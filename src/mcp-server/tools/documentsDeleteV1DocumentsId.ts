/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentsDeleteV1DocumentsId } from "../../funcs/documentsDeleteV1DocumentsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteV1DocumentsIdRequest$inboundSchema,
};

export const tool$documentsDeleteV1DocumentsId: ToolDefinition<typeof args> = {
  name: "documents-delete-v1-documents-id",
  description: `Delete a document

Delete a document by its unique identifier for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentsDeleteV1DocumentsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
