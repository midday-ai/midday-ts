/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentsGetPreSignedUrl } from "../../funcs/documentsGetPreSignedUrl.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetDocumentPreSignedUrlRequest$inboundSchema,
};

export const tool$documentsGetPreSignedUrl: ToolDefinition<typeof args> = {
  name: "documents-get-pre-signed-url",
  description: `Generate pre-signed URL for document

Generate a pre-signed URL for accessing a document. The URL is valid for 60 seconds and allows secure temporary access to the document file.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentsGetPreSignedUrl(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
