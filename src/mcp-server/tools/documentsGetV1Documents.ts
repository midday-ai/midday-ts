/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentsGetV1Documents } from "../../funcs/documentsGetV1Documents.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1DocumentsRequest$inboundSchema,
};

export const tool$documentsGetV1Documents: ToolDefinition<typeof args> = {
  name: "documents-get-v1-documents",
  description: `List all documents

Retrieve a list of documents for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentsGetV1Documents(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
