/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentsGetV1DocumentsId } from "../../funcs/documentsGetV1DocumentsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1DocumentsIdRequest$inboundSchema,
};

export const tool$documentsGetV1DocumentsId: ToolDefinition<typeof args> = {
  name: "documents-get-v1-documents-id",
  description: `Retrieve a document

Retrieve a document by its unique identifier for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentsGetV1DocumentsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
