/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { filesDownloadFile } from "../../funcs/filesDownloadFile.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DownloadFileRequest$inboundSchema,
};

export const tool$filesDownloadFile: ToolDefinition<typeof args> = {
  name: "files-download-file",
  description: `Download file from vault

Downloads a file from the vault storage bucket. Requires team file key (fk) query parameter for access.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await filesDownloadFile(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
