/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { filesDownloadInvoice } from "../../funcs/filesDownloadInvoice.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DownloadInvoiceRequest$inboundSchema,
};

export const tool$filesDownloadInvoice: ToolDefinition<typeof args> = {
  name: "files-download-invoice",
  description: `Download invoice PDF

Downloads an invoice as a PDF. Can be accessed with an invoice ID (requires team file key via fk query parameter) or invoice token (public access).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await filesDownloadInvoice(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
