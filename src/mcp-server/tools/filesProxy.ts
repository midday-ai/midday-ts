/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { filesProxy } from "../../funcs/filesProxy.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ProxyFileRequest$inboundSchema,
};

export const tool$filesProxy: ToolDefinition<typeof args> = {
  name: "files-proxy",
  description: `Proxy file from storage

Proxies a file from storage. Requires team file key (fk) query parameter for access.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await filesProxy(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
