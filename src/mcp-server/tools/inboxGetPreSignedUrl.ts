/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { inboxGetPreSignedUrl } from "../../funcs/inboxGetPreSignedUrl.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetInboxPreSignedUrlRequest$inboundSchema,
};

export const tool$inboxGetPreSignedUrl: ToolDefinition<typeof args> = {
  name: "inbox-get-pre-signed-url",
  description: `Generate pre-signed URL for inbox attachment

Generate a pre-signed URL for accessing an inbox attachment. The URL is valid for 60 seconds and allows secure temporary access to the attachment file.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await inboxGetPreSignedUrl(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
