/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { inboxGetV1Inbox } from "../../funcs/inboxGetV1Inbox.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1InboxRequest$inboundSchema,
};

export const tool$inboxGetV1Inbox: ToolDefinition<typeof args> = {
  name: "inbox-get-v1-inbox",
  description: `List all inbox items

Retrieve a list of inbox items for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await inboxGetV1Inbox(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
