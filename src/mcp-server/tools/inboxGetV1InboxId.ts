/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { inboxGetV1InboxId } from "../../funcs/inboxGetV1InboxId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1InboxIdRequest$inboundSchema,
};

export const tool$inboxGetV1InboxId: ToolDefinition<typeof args> = {
  name: "inbox-get-v1-inbox-id",
  description: `Retrieve a inbox item

Retrieve a inbox item by its unique identifier for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await inboxGetV1InboxId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
