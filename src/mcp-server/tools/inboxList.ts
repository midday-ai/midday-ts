/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { inboxList } from "../../funcs/inboxList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListInboxItemsRequest$inboundSchema,
};

export const tool$inboxList: ToolDefinition<typeof args> = {
  name: "inbox-list",
  description: `List all inbox items

Retrieve a list of inbox items for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await inboxList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
