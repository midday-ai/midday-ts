/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { inboxPatchV1InboxId } from "../../funcs/inboxPatchV1InboxId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchV1InboxIdRequest$inboundSchema,
};

export const tool$inboxPatchV1InboxId: ToolDefinition<typeof args> = {
  name: "inbox-patch-v1-inbox-id",
  description: `Update a inbox item

Update fields of an inbox item by its unique identifier for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await inboxPatchV1InboxId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
