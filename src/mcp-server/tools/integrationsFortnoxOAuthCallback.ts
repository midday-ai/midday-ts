/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsFortnoxOAuthCallback } from "../../funcs/integrationsFortnoxOAuthCallback.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.FortnoxOAuthCallbackRequest$inboundSchema,
};

export const tool$integrationsFortnoxOAuthCallback: ToolDefinition<
  typeof args
> = {
  name: "integrations-fortnox-O-auth-callback",
  description: `Fortnox OAuth callback

Handles OAuth callback from Fortnox after user authorization. Exchanges authorization code for access token and creates app integration.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsFortnoxOAuthCallback(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
