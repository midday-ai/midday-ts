/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsGetFortnoxInstallUrl } from "../../funcs/integrationsGetFortnoxInstallUrl.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$integrationsGetFortnoxInstallUrl: ToolDefinition = {
  name: "integrations-get-fortnox-install-url",
  description: `Get Fortnox install URL

Generates OAuth install URL for Fortnox integration. Requires authentication.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await integrationsGetFortnoxInstallUrl(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
