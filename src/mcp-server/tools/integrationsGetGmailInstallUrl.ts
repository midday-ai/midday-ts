/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsGetGmailInstallUrl } from "../../funcs/integrationsGetGmailInstallUrl.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$integrationsGetGmailInstallUrl: ToolDefinition = {
  name: "integrations-get-gmail-install-url",
  description: `Get Gmail install URL

Generates OAuth install URL for Gmail integration. Requires authentication.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await integrationsGetGmailInstallUrl(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
