/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsGetQuickBooksInstallUrl } from "../../funcs/integrationsGetQuickBooksInstallUrl.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$integrationsGetQuickBooksInstallUrl: ToolDefinition = {
  name: "integrations-get-quick-books-install-url",
  description: `Get QuickBooks install URL

Generates OAuth install URL for QuickBooks integration. Requires authentication.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await integrationsGetQuickBooksInstallUrl(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
