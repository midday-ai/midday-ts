/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsGetSlackInstallUrl } from "../../funcs/integrationsGetSlackInstallUrl.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$integrationsGetSlackInstallUrl: ToolDefinition = {
  name: "integrations-get-slack-install-url",
  description: `Get Slack install URL

Generates OAuth install URL for Slack integration. Requires authentication.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await integrationsGetSlackInstallUrl(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
