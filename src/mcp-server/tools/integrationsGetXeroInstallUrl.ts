/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsGetXeroInstallUrl } from "../../funcs/integrationsGetXeroInstallUrl.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$integrationsGetXeroInstallUrl: ToolDefinition = {
  name: "integrations-get-xero-install-url",
  description: `Get Xero install URL

Generates OAuth install URL for Xero integration. Requires authentication.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await integrationsGetXeroInstallUrl(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
