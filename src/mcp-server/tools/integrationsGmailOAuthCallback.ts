/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsGmailOAuthCallback } from "../../funcs/integrationsGmailOAuthCallback.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GmailOAuthCallbackRequest$inboundSchema,
};

export const tool$integrationsGmailOAuthCallback: ToolDefinition<typeof args> =
  {
    name: "integrations-gmail-O-auth-callback",
    description: `Gmail OAuth callback

Handles OAuth callback from Google after user authorization. Exchanges authorization code for access token and creates inbox account.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await integrationsGmailOAuthCallback(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
