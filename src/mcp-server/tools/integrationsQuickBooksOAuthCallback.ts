/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsQuickBooksOAuthCallback } from "../../funcs/integrationsQuickBooksOAuthCallback.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.QuickBooksOAuthCallbackRequest$inboundSchema,
};

export const tool$integrationsQuickBooksOAuthCallback: ToolDefinition<
  typeof args
> = {
  name: "integrations-quick-books-O-auth-callback",
  description: `QuickBooks OAuth callback

Handles OAuth callback from QuickBooks after user authorization. Exchanges authorization code for access token and creates app integration.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsQuickBooksOAuthCallback(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
