/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsSlackInteractions } from "../../funcs/integrationsSlackInteractions.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$integrationsSlackInteractions: ToolDefinition = {
  name: "integrations-slack-interactions",
  description: `Slack interactions handler

Handles interactive component actions from Slack (button clicks, etc.)`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await integrationsSlackInteractions(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
