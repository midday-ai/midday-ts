/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsSlackOAuthCallback } from "../../funcs/integrationsSlackOAuthCallback.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SlackOAuthCallbackRequest$inboundSchema,
};

export const tool$integrationsSlackOAuthCallback: ToolDefinition<typeof args> =
  {
    name: "integrations-slack-O-auth-callback",
    description: `Slack OAuth callback

Handles OAuth callback from Slack after user authorization. Exchanges authorization code for access token and creates app integration.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await integrationsSlackOAuthCallback(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
