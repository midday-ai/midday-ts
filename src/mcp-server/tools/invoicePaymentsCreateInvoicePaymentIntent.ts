/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { invoicePaymentsCreateInvoicePaymentIntent } from "../../funcs/invoicePaymentsCreateInvoicePaymentIntent.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateInvoicePaymentIntentRequest$inboundSchema,
};

export const tool$invoicePaymentsCreateInvoicePaymentIntent: ToolDefinition<
  typeof args
> = {
  name: "invoice-payments-create-invoice-payment-intent",
  description: `Create payment intent for invoice

Creates a Stripe PaymentIntent for paying an invoice. This is a public endpoint that uses the invoice token for authentication.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await invoicePaymentsCreateInvoicePaymentIntent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
