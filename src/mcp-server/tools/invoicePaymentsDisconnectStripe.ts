/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { invoicePaymentsDisconnectStripe } from "../../funcs/invoicePaymentsDisconnectStripe.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$invoicePaymentsDisconnectStripe: ToolDefinition = {
  name: "invoice-payments-disconnect-stripe",
  description: `Disconnect Stripe account

Disconnects the team's Stripe Connect account.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await invoicePaymentsDisconnectStripe(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
