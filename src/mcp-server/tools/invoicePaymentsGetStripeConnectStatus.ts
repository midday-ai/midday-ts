/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { invoicePaymentsGetStripeConnectStatus } from "../../funcs/invoicePaymentsGetStripeConnectStatus.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$invoicePaymentsGetStripeConnectStatus: ToolDefinition = {
  name: "invoice-payments-get-stripe-connect-status",
  description: `Get Stripe Connect status

Gets the current Stripe Connect status for the team.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await invoicePaymentsGetStripeConnectStatus(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
