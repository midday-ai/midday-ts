/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { invoicePaymentsGetStripeConnectUrl } from "../../funcs/invoicePaymentsGetStripeConnectUrl.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$invoicePaymentsGetStripeConnectUrl: ToolDefinition = {
  name: "invoice-payments-get-stripe-connect-url",
  description: `Get Stripe Connect URL

Generates OAuth URL for Stripe Connect Standard integration. Allows teams to connect their Stripe account for accepting invoice payments.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await invoicePaymentsGetStripeConnectUrl(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
