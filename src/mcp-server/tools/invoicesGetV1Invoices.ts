/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { invoicesGetV1Invoices } from "../../funcs/invoicesGetV1Invoices.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1InvoicesRequest$inboundSchema,
};

export const tool$invoicesGetV1Invoices: ToolDefinition<typeof args> = {
  name: "invoices-get-v1-invoices",
  description: `List all invoices

Retrieve a list of invoices for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await invoicesGetV1Invoices(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
