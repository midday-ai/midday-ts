/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { invoicesGetV1InvoicesPaymentStatus } from "../../funcs/invoicesGetV1InvoicesPaymentStatus.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$invoicesGetV1InvoicesPaymentStatus: ToolDefinition = {
  name: "invoices-get-v1-invoices-payment-status",
  description: `Payment status

Get payment status for the authenticated team.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await invoicesGetV1InvoicesPaymentStatus(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
