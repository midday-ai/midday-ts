/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { invoicesGetV1InvoicesSummary } from "../../funcs/invoicesGetV1InvoicesSummary.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1InvoicesSummaryRequest$inboundSchema,
};

export const tool$invoicesGetV1InvoicesSummary: ToolDefinition<typeof args> = {
  name: "invoices-get-v1-invoices-summary",
  description: `Invoice summary

Get summary of invoices for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await invoicesGetV1InvoicesSummary(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
