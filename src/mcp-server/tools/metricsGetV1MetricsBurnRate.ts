/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsGetV1MetricsBurnRate } from "../../funcs/metricsGetV1MetricsBurnRate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1MetricsBurnRateRequest$inboundSchema,
};

export const tool$metricsGetV1MetricsBurnRate: ToolDefinition<typeof args> = {
  name: "metrics-get-v1-metrics-burn-rate",
  description: `Burn rate metrics

Burn rate metrics for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsGetV1MetricsBurnRate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
