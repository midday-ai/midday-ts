/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsSpending } from "../../funcs/metricsSpending.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSpendingMetricsRequest$inboundSchema,
};

export const tool$metricsSpending: ToolDefinition<typeof args> = {
  name: "metrics-spending",
  description: `Spending metrics

Spending metrics for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsSpending(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
