/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { notificationsUpdateAllStatus } from "../../funcs/notificationsUpdateAllStatus.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.UpdateAllNotificationsStatusSchema$inboundSchema.optional(),
};

export const tool$notificationsUpdateAllStatus: ToolDefinition<typeof args> = {
  name: "notifications-update-all-status",
  description: `Update status of all notifications

Update the status of all notifications for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await notificationsUpdateAllStatus(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
