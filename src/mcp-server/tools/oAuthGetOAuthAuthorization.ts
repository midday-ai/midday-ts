/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oAuthGetOAuthAuthorization } from "../../funcs/oAuthGetOAuthAuthorization.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOAuthAuthorizationRequest$inboundSchema,
};

export const tool$oAuthGetOAuthAuthorization: ToolDefinition<typeof args> = {
  name: "O-auth-get-O-auth-authorization",
  description: `OAuth Authorization Endpoint

Initiate OAuth authorization flow and get consent screen information`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await oAuthGetOAuthAuthorization(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
