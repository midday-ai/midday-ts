/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oAuthPostOAuthAuthorization } from "../../funcs/oAuthPostOAuthAuthorization.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostOAuthAuthorizationRequest$inboundSchema.optional(),
};

export const tool$oAuthPostOAuthAuthorization: ToolDefinition<typeof args> = {
  name: "O-auth-post-O-auth-authorization",
  description: `OAuth Authorization Decision

Process user's authorization decision (allow/deny)`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await oAuthPostOAuthAuthorization(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
