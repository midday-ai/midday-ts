/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oAuthPostOAuthRegister } from "../../funcs/oAuthPostOAuthRegister.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostOAuthRegisterRequest$inboundSchema,
};

export const tool$oAuthPostOAuthRegister: ToolDefinition<typeof args> = {
  name: "O-auth-post-O-auth-register",
  description: `Dynamic Client Registration

Register an OAuth client dynamically (RFC 7591). Used by MCP clients like ChatGPT and Claude.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await oAuthPostOAuthRegister(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
