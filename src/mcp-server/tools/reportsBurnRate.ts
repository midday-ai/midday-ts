/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { reportsBurnRate } from "../../funcs/reportsBurnRate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetBurnRateReportsRequest$inboundSchema,
};

export const tool$reportsBurnRate: ToolDefinition<typeof args> = {
  name: "reports-burn-rate",
  description: `Burn rate reports

Burn rate reports for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await reportsBurnRate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
