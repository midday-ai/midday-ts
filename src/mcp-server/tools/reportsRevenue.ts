/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { reportsRevenue } from "../../funcs/reportsRevenue.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetRevenueReportsRequest$inboundSchema,
};

export const tool$reportsRevenue: ToolDefinition<typeof args> = {
  name: "reports-revenue",
  description: `Revenue reports

Revenue reports for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await reportsRevenue(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
