/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { reportsRunway } from "../../funcs/reportsRunway.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetRunwayReportsRequest$inboundSchema,
};

export const tool$reportsRunway: ToolDefinition<typeof args> = {
  name: "reports-runway",
  description: `Runway reports

Runway reports for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await reportsRunway(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
