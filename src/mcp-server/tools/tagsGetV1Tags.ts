/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tagsGetV1Tags } from "../../funcs/tagsGetV1Tags.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$tagsGetV1Tags: ToolDefinition = {
  name: "tags-get-v1-tags",
  description: `List all tags

Retrieve a list of tags for the authenticated team.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await tagsGetV1Tags(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
