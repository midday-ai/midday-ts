/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { teamsGetV1Teams } from "../../funcs/teamsGetV1Teams.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$teamsGetV1Teams: ToolDefinition = {
  name: "teams-get-v1-teams",
  description: `List all teams

Retrieve a list of teams for the authenticated user.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await teamsGetV1Teams(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
