/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { teamsGetV1TeamsId } from "../../funcs/teamsGetV1TeamsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1TeamsIdRequest$inboundSchema,
};

export const tool$teamsGetV1TeamsId: ToolDefinition<typeof args> = {
  name: "teams-get-v1-teams-id",
  description: `Retrieve a team

Retrieve a team by its ID for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await teamsGetV1TeamsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
