/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { teamsMembers } from "../../funcs/teamsMembers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListTeamMembersRequest$inboundSchema,
};

export const tool$teamsMembers: ToolDefinition<typeof args> = {
  name: "teams-members",
  description: `List all team members

List all team members for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await teamsMembers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
