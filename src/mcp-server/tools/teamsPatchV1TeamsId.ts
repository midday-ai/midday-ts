/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { teamsPatchV1TeamsId } from "../../funcs/teamsPatchV1TeamsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchV1TeamsIdRequest$inboundSchema,
};

export const tool$teamsPatchV1TeamsId: ToolDefinition<typeof args> = {
  name: "teams-patch-v1-teams-id",
  description: `Update a team

Update a team for the authenticated workspace. If there’s no change, returns it as it is.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await teamsPatchV1TeamsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
