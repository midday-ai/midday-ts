/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { trackerDeleteV1TrackerEntriesId } from "../../funcs/trackerDeleteV1TrackerEntriesId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteV1TrackerEntriesIdRequest$inboundSchema,
};

export const tool$trackerDeleteV1TrackerEntriesId: ToolDefinition<typeof args> =
  {
    name: "tracker-delete-v1-tracker-entries-id",
    description: `Delete a tracker entry

Delete a tracker entry for the authenticated team.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await trackerDeleteV1TrackerEntriesId(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
