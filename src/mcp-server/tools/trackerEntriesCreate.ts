/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { trackerEntriesCreate } from "../../funcs/trackerEntriesCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateTrackerEntryRequest$inboundSchema.optional(),
};

export const tool$trackerEntriesCreate: ToolDefinition<typeof args> = {
  name: "tracker-entries-create",
  description: `Create a tracker entry

Create a tracker entry for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await trackerEntriesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
