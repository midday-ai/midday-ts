/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { trackerEntriesCreateBulk } from "../../funcs/trackerEntriesCreateBulk.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateTrackerEntriesBulkRequest$inboundSchema.optional(),
};

export const tool$trackerEntriesCreateBulk: ToolDefinition<typeof args> = {
  name: "tracker-entries-create-bulk",
  description: `Create multiple tracker entries

Create multiple tracker entries in a single request for efficient data migration.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await trackerEntriesCreateBulk(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
