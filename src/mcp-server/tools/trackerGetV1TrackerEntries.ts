/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { trackerGetV1TrackerEntries } from "../../funcs/trackerGetV1TrackerEntries.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1TrackerEntriesRequest$inboundSchema,
};

export const tool$trackerGetV1TrackerEntries: ToolDefinition<typeof args> = {
  name: "tracker-get-v1-tracker-entries",
  description: `List all tracker entries

List all tracker entries for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await trackerGetV1TrackerEntries(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
