/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { trackerGetV1TrackerProjects } from "../../funcs/trackerGetV1TrackerProjects.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1TrackerProjectsRequest$inboundSchema,
};

export const tool$trackerGetV1TrackerProjects: ToolDefinition<typeof args> = {
  name: "tracker-get-v1-tracker-projects",
  description: `List all tracker projects

List all tracker projects for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await trackerGetV1TrackerProjects(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
