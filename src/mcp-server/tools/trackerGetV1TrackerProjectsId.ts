/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { trackerGetV1TrackerProjectsId } from "../../funcs/trackerGetV1TrackerProjectsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1TrackerProjectsIdRequest$inboundSchema,
};

export const tool$trackerGetV1TrackerProjectsId: ToolDefinition<typeof args> = {
  name: "tracker-get-v1-tracker-projects-id",
  description: `Retrieve a tracker project

Retrieve a tracker project for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await trackerGetV1TrackerProjectsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
