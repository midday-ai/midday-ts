/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { trackerPatchV1TrackerProjectsId } from "../../funcs/trackerPatchV1TrackerProjectsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchV1TrackerProjectsIdRequest$inboundSchema,
};

export const tool$trackerPatchV1TrackerProjectsId: ToolDefinition<typeof args> =
  {
    name: "tracker-patch-v1-tracker-projects-id",
    description: `Update a tracker project

Update a tracker project for the authenticated team.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await trackerPatchV1TrackerProjectsId(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
