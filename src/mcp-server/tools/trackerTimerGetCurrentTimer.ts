/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { trackerTimerGetCurrentTimer } from "../../funcs/trackerTimerGetCurrentTimer.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCurrentTimerRequest$inboundSchema,
};

export const tool$trackerTimerGetCurrentTimer: ToolDefinition<typeof args> = {
  name: "tracker-timer-get-current-timer",
  description: `Get current timer

Get the currently running timer for the authenticated user.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await trackerTimerGetCurrentTimer(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
