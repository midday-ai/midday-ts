/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { trackerTimerStartTimer } from "../../funcs/trackerTimerStartTimer.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.StartTimerRequest$inboundSchema.optional(),
};

export const tool$trackerTimerStartTimer: ToolDefinition<typeof args> = {
  name: "tracker-timer-start-timer",
  description: `Start a timer

Start a new timer or continue from a paused entry.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await trackerTimerStartTimer(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
