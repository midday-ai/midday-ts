/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { trackerTimerStopTimer } from "../../funcs/trackerTimerStopTimer.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.StopTimerRequest$inboundSchema.optional(),
};

export const tool$trackerTimerStopTimer: ToolDefinition<typeof args> = {
  name: "tracker-timer-stop-timer",
  description: `Stop a timer

Stop the current running timer or a specific timer entry.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await trackerTimerStopTimer(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
