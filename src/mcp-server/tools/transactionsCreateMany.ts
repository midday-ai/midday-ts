/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { transactionsCreateMany } from "../../funcs/transactionsCreateMany.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: z.array(operations.RequestBody$inboundSchema).optional(),
};

export const tool$transactionsCreateMany: ToolDefinition<typeof args> = {
  name: "transactions-create-many",
  description: `Bulk create transactions

Bulk create transactions for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transactionsCreateMany(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
