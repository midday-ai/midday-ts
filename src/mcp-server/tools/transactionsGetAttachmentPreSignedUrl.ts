/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transactionsGetAttachmentPreSignedUrl } from "../../funcs/transactionsGetAttachmentPreSignedUrl.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTransactionAttachmentPreSignedUrlRequest$inboundSchema,
};

export const tool$transactionsGetAttachmentPreSignedUrl: ToolDefinition<
  typeof args
> = {
  name: "transactions-get-attachment-pre-signed-url",
  description: `Generate pre-signed URL for transaction attachment

Generate a pre-signed URL for accessing a transaction attachment. The URL is valid for 60 seconds and allows secure temporary access to the attachment file.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transactionsGetAttachmentPreSignedUrl(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
