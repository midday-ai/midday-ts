/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transactionsGetV1Transactions } from "../../funcs/transactionsGetV1Transactions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1TransactionsRequest$inboundSchema,
};

export const tool$transactionsGetV1Transactions: ToolDefinition<typeof args> = {
  name: "transactions-get-v1-transactions",
  description: `List all transactions

Retrieve a list of transactions for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transactionsGetV1Transactions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
