/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transactionsPatchV1TransactionsBulk } from "../../funcs/transactionsPatchV1TransactionsBulk.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchV1TransactionsBulkRequest$inboundSchema.optional(),
};

export const tool$transactionsPatchV1TransactionsBulk: ToolDefinition<
  typeof args
> = {
  name: "transactions-patch-v1-transactions-bulk",
  description: `Bulk update transactions

Bulk update transactions for the authenticated team. If there's no change, returns it as it is.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transactionsPatchV1TransactionsBulk(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
