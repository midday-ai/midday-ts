/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transactionsPostV1Transactions } from "../../funcs/transactionsPostV1Transactions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostV1TransactionsRequest$inboundSchema.optional(),
};

export const tool$transactionsPostV1Transactions: ToolDefinition<typeof args> =
  {
    name: "transactions-post-v1-transactions",
    description: `Create a transaction

Create a transaction`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await transactionsPostV1Transactions(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
