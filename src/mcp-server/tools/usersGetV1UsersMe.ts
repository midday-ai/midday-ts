/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersGetV1UsersMe } from "../../funcs/usersGetV1UsersMe.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$usersGetV1UsersMe: ToolDefinition = {
  name: "users-get-v1-users-me",
  description: `Retrieve the current user

Retrieve the current user for the authenticated team.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await usersGetV1UsersMe(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
