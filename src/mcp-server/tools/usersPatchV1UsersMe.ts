/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersPatchV1UsersMe } from "../../funcs/usersPatchV1UsersMe.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchV1UsersMeRequest$inboundSchema.optional(),
};

export const tool$usersPatchV1UsersMe: ToolDefinition<typeof args> = {
  name: "users-patch-v1-users-me",
  description: `Update the current user

Update the current user for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersPatchV1UsersMe(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
