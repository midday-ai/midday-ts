/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersUpdate } from "../../funcs/usersUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateCurrentUserRequest$inboundSchema.optional(),
};

export const tool$usersUpdate: ToolDefinition<typeof args> = {
  name: "users-update",
  description: `Update the current user

Update the current user for the authenticated team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
