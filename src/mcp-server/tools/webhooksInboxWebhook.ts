/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { webhooksInboxWebhook } from "../../funcs/webhooksInboxWebhook.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: z.any(),
};

export const tool$webhooksInboxWebhook: ToolDefinition<typeof args> = {
  name: "webhooks-inbox-webhook",
  description: `Inbox webhook

Webhook endpoint for receiving inbox emails from Postmark`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhooksInboxWebhook(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
