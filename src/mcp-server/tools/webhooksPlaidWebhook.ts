/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksPlaidWebhook } from "../../funcs/webhooksPlaidWebhook.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$webhooksPlaidWebhook: ToolDefinition = {
  name: "webhooks-plaid-webhook",
  description: `Plaid webhook handler

Handles Plaid webhook events for transaction updates and item status changes. Verifies the Plaid-Verification JWT signature.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await webhooksPlaidWebhook(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
