/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksStripeWebhook } from "../../funcs/webhooksStripeWebhook.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$webhooksStripeWebhook: ToolDefinition = {
  name: "webhooks-stripe-webhook",
  description: `Stripe webhook handler

Handles Stripe webhook events for invoice payments. Verifies webhook signature and processes payment events.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await webhooksStripeWebhook(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
