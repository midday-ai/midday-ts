/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksTellerWebhook } from "../../funcs/webhooksTellerWebhook.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$webhooksTellerWebhook: ToolDefinition = {
  name: "webhooks-teller-webhook",
  description: `Teller webhook handler

Handles Teller webhook events for enrollment disconnections and transaction updates.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await webhooksTellerWebhook(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
