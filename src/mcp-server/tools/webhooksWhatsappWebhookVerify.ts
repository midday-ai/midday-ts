/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksWhatsappWebhookVerify } from "../../funcs/webhooksWhatsappWebhookVerify.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.WhatsappWebhookVerifyRequest$inboundSchema,
};

export const tool$webhooksWhatsappWebhookVerify: ToolDefinition<typeof args> = {
  name: "webhooks-whatsapp-webhook-verify",
  description: `WhatsApp webhook verification

Verify webhook URL for WhatsApp Business API`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhooksWhatsappWebhookVerify(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
