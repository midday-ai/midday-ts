/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type RevenueSummary = {
  /**
   * Total revenue for the current period
   */
  currentTotal: number;
  /**
   * Total revenue for the previous period
   */
  prevTotal: number;
  /**
   * Currency code (ISO 4217)
   */
  currency: string;
};

/** @internal */
export const RevenueSummary$inboundSchema: z.ZodType<
  RevenueSummary,
  z.ZodTypeDef,
  unknown
> = z.object({
  currentTotal: z.number(),
  prevTotal: z.number(),
  currency: z.string(),
});
/** @internal */
export type RevenueSummary$Outbound = {
  currentTotal: number;
  prevTotal: number;
  currency: string;
};

/** @internal */
export const RevenueSummary$outboundSchema: z.ZodType<
  RevenueSummary$Outbound,
  z.ZodTypeDef,
  RevenueSummary
> = z.object({
  currentTotal: z.number(),
  prevTotal: z.number(),
  currency: z.string(),
});

export function revenueSummaryToJSON(revenueSummary: RevenueSummary): string {
  return JSON.stringify(RevenueSummary$outboundSchema.parse(revenueSummary));
}
export function revenueSummaryFromJSON(
  jsonString: string,
): SafeParseResult<RevenueSummary, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RevenueSummary$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RevenueSummary' from JSON`,
  );
}
